# VectorForge

A retrieval-augmented generation (RAG) engine built from scratch — no Pinecone, no LangChain. Upload documents, ask questions, get accurate answers with citations.

Built with Python + FastAPI. Cosine similarity is hand-written; the vector store sits behind an interface so it scales to pgvector without changing the pipeline.

🚧 Building in public, phase by phase.
